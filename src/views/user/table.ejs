<table class="table" id="table1">
	<thead>
		<tr>
			<th width="5%">No.</th>
			<th>Nama</th>
			<th>Nama Pegawai</th>
			<th>Jenis Akun</th>
			<th width="15%">Aksi</th>
		</tr>
	</thead>
	<tbody>
		<% for(let i = 0 ; i < users.length; i++ ) {%>
		<tr>
			<td><%= i+1 %></td>
			<td><%= users[i].name %></td>
			<td><%= users[i].name %></td>
			<td><%= users[i].role %></td>
			<td>
				<a
					href="/<%= current%>/<%= users[i]._id %>/edit"
					class="btn btn-warning btn-sm"
					>Edit</a
				>
				<form
					action="/<%= current%>/delete/<%= users[i]._id %>?_method=DELETE"
					method="POST"
				></form>
				<button class="btn btn-sm btn-danger mt-1 ml-1" id="btn-delete">
					Hapus
				</button>
			</td>
		</tr>
		<%}%>
	</tbody>
</table>
